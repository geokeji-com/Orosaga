# 实施状态与上线边界

基线日期：2026-07-22。本文区分仓库内已经实现并验证的能力、必须使用真实外部凭据完成的控制面操作，以及正式生产放量前仍需业务确认的事项。

## 已实现并验证

| 阶段         | 已完成证据                                                                                                                                                                 |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0 基线       | 首发已完成30条旧员工扩展资料、31个资产、16个营地、253个旧口径节点、7个系统入口、6个阶段和18项完成标准的迁移对账；可识别迁移源随后从服务器 release 与公开仓库当前树删除。   |
| 1 单仓底座   | npm workspaces、Node/npm 精确版本、Web/API/Worker/Contracts/Config/Testing 分层、PostgreSQL Compose、Nginx、三份 Dockerfile、健康检查、JSON 日志和环境变量启动校验已建立。 |
| 2 身份权限   | 飞书 OAuth code 流、服务端 session 摘要、HttpOnly/Secure/SameSite Cookie、CSRF、三级 RBAC、停用会话撤销、受保护头像 API 和匿名默认拒绝已实现。                             |
| 3 内容迁移   | 首页、公司页、导航、组织扩展资料、工作流、系统入口和营地基线已一次性导入；生产 seed 已退役，内容与结构化资源继续通过版本冲突、不可变 revision、审计和回滚维护。            |
| 4 飞书同步   | 官方 Node SDK、组织全量读取、Wiki 深层分页、明确 token 排除、稳定营地编号、双统计口径、事务切换、软删除、超时、受限重试和 PostgreSQL advisory lock 已实现。                |
| 5 Web API 化 | 正式路由、鉴权守卫、404、懒加载、服务端搜索/天气、API 驱动页面、可访问搜索与弹窗、后台立即发布和知识源配置已实现；Web bundle 不含人员名单、节点 token、内部地址或头像。    |
| 6 发布基础   | CI 验证、CodeQL、依赖审计、secret/license/bundle/SQL 注释门禁、SBOM、生产 Compose、回环灰度、精确 Nginx vhost、acme.sh 安装模板和回退手册已经入库。                        |

## 本地验证结果

- 全新 PostgreSQL 验证库可从零执行 migration 和 seed，19 张业务表及字段 SQL 注释缺失数为 0。
- API smoke 覆盖匿名拒绝、三角色 200/403、CSRF、统一校验错误、立即发布、409、知识源版本、搜索、退出与会话撤销。
- 34 项 Playwright 稳定通过，包含 6 个页面在 1440、1024、768、390、320px 的 30 份视觉基线、真实 404、员工弹窗键盘关闭、后台立即发布和首页严重/致命无障碍扫描。
- lint、Prettier、TypeScript、单元/契约覆盖率、production build、bundle budget、敏感 bundle、仓库 secret、许可证和 npm 官方漏洞审计均通过；npm audit 为 0 vulnerability。

## 当前生产状态与外部边界

以下状态来自2026-07-22真实生产发布；后续控制面变更仍需对应企业或云权限：

| 工作       | 当前边界                                                                                                                                      |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| GitHub     | 仓库按协作需要保持 Public；生产迁移与修复 PR 已在完整 CI 通过后合并，公开树不得新增凭据、员工资料、头像或真实 Wiki token。                    |
| 飞书应用   | 单个企业自建应用已完成网页登录、通讯录和 Wiki 真实验证；最近完整快照为7个部门、33位活动用户、18个直属 Wiki 节点，排除2个后展示16个营地。      |
| 阿里云资源 | ECS 正在运行合并 SHA 镜像；私有 OSS 匿名请求返回403；RDS 使用显式授权的同 VPC 明文连接，迁移前手工物理备份已成功。PITR 仍按本轮决策保持关闭。 |
| 头像迁移   | 31张头像已逐对象上传并核对 hash、MIME、大小；浏览器只通过登录态同源 API 获取，不暴露 OSS 地址。                                               |
| CI/CD 投递 | 手工首发稳定后，再接入 commit-SHA 制品、专用 deploy key、人工生产批准和 smoke 自动回退。                                                      |
| 业务治理   | 确认内容负责人、公司效果数据来源日期、系统入口域名白名单，并为两个被禁用的不安全旧入口提供 HTTPS 云端地址。                                   |
| 灰度与灾备 | 本机、服务器回环与独立 HTTPS 域名验证均已通过；恢复锚点为上线前手工快照和自动备份。独立 staging、真实恢复与性能演练仍待补齐。                 |

## 上线后仍需关闭的工程闸门

- Orosaga 容器、`orosaga` schema、独立 Nginx vhost 与 TLS 已上线；后续数据库变更仍必须先确认备份锚点，并继续执行内网免 SSL、schema 和 `pg_trgm` 预检。
- 当前覆盖率闸门证明已纳入测试的核心模块达到阈值，不等同于整个仓库所有源文件达到 80%；应在上线前扩大采集范围并关闭剩余分支缺口。
- 后台已覆盖页面发布、历史回滚、知识源和同步；工作流、系统入口、员工扩展资料目前具备受保护 API，完整专用表单和受限 block editor 仍需补齐。
- 组织与 Wiki 的真实租户完整同步、姓名唯一迁移、明确排除和旧口径对账已完成；仍需持续监控分页、权限失败、同步新鲜度和停用会话撤销。
- 99.9% 可用性、API/搜索 p95、RPO/RTO 和连续运行指标只能在 staging/production 通过监控与演练确认。

当前结论为已生产上线但带后续条件：凭据轮换、KMS/SLS、独立 staging、恢复演练与性能指标验证仍需单独完成。
